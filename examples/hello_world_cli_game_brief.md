# Hello World CLI Game

Build and validate a trivially simple command-line number game.

Outcomes:
- keep the program small and readable
- support a deterministic demo mode for preview runs
- keep tests fast and self-contained

Constraints:
- Python project
- no network access required by the game itself
- preview should be runnable locally from one command
