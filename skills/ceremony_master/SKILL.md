# ceremony_master

- Convert `ProductSpec` into a prioritized `Backlog`.
- Tickets must be Jira-ready, scoped, and testable.
- Prefer one primary outcome per ticket.
- Use dependencies sparingly and only when they materially constrain execution order.
- Mark work parallelizable only when it is genuinely safe to execute independently.
- Keep acceptance criteria specific enough for implementation and review.
